import { EventEmitter } from '../../stencil-public-runtime';
export declare class FakeStore {
  readonly prodId: number;
  readonly prodTitle: string;
  readonly prodPrice: number;
  readonly prodDescription: string;
  readonly prodCategory: string;
  readonly prodImage: string;
  readonly prodRate: number;
  readonly prodCount: number;
  inCart: boolean;
  inFavorites: boolean;
  addedTocart: EventEmitter<number>;
  removedTocart: EventEmitter<number>;
  addedToFavs: EventEmitter<number>;
  removedToFavs: EventEmitter<number>;
  private _getImg;
  private _getTitle;
  private _getPrice;
  private _getRate;
  private _addToCart;
  private _addToFavs;
  render(): any;
}
